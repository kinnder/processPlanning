package planning.storage;

public class SystemProcessXMLFileTest {

	// TODO : add tests

	/*
	 * SystemProcessXMLFile xmlFile = new SystemProcessXMLFile();
	 * xmlFile.setProcess(operations); try { xmlFile.save(new
	 * URL("file:///D:/process.xml")); } catch (IOException | URISyntaxException e)
	 * { e.printStackTrace(); }
	 */
}
